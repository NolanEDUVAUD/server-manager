import type { Dict } from "..";

export const settings: Dict["settings"] = {
  language: {
    title: "Language",
    label: "Interface language",
    help: "Applies immediately. Some detailed system messages remain in French.",
  },
};
